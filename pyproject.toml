[tool.poetry]
name = "knowledgeminer"
version = "0.1.0"
description = ""
authors = ["arsalangundroo"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
llama-index = "^0.9.34"
langchain = "^0.1.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
